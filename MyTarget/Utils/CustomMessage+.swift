//
//  CustomMessage+.swift
//  MyTarget
//
//  Created by 邵明易 on 2023/3/16.
//
//import SwiftMessages
import SwiftUI

func popSuccessMessage(icon: String, title: String, body: String){
       
        /*let view = MessageView.viewFromNib(layout: .cardView)
 
        // Theme message elements with the warning style.
        view.configureTheme(.info)
 
        // Add a drop shadow.
        view.configureDropShadow()
 
        // Set message title, body, and icon. Here, we're overriding the default warning
        // image with an emoji character.
       
        //隐藏按钮
        view.button?.isHidden = true
    
        //let iconText = [""].randomElement()!
    
        view.configureContent(title: title, body: body, iconImage: UIImage(systemName: "checkmark.message"), iconText: "" , buttonImage: UIImage(systemName:"checkmark"), buttonTitle: .none, buttonTapHandler: .none)
        
 
        // Increase the external margin around the card. In general, the effect of this setting
        // depends on how the given layout is constrained to the layout margins.
        view.layoutMarginAdditions = UIEdgeInsets(top: 20, left: 20, bottom: 20, right: 20)
 
        // Reduce the corner radius (applicable to layouts featuring rounded corners).
        (view.backgroundView as? CornerRoundingView)?.cornerRadius = 10
 
        // Show the message.
        SwiftMessages.show(view: view)
        */
    }


func popTipMessage(icon: String, title: String, body: String){
       
        /*let view = MessageView.viewFromNib(layout: .cardView)
 
        // Theme message elements with the warning style.
        view.configureTheme(.info)
 
        // Add a drop shadow.
        view.configureDropShadow()
 
        // Set message title, body, and icon. Here, we're overriding the default warning
        // image with an emoji character.
       
        //隐藏按钮
        view.button?.isHidden = true
    
        //let iconText = [""].randomElement()!
        
        view.configureContent(title: title, body: body,iconImage: UIImage(systemName: "ellipsis.message"), iconText: "" , buttonImage: UIImage(systemName:"checkmark"), buttonTitle: .none, buttonTapHandler: .none)
        
 
        // Increase the external margin around the card. In general, the effect of this setting
        // depends on how the given layout is constrained to the layout margins.
        view.layoutMarginAdditions = UIEdgeInsets(top: 20, left: 20, bottom: 20, right: 20)
 
        // Reduce the corner radius (applicable to layouts featuring rounded corners).
        (view.backgroundView as? CornerRoundingView)?.cornerRadius = 10
 
        // Show the message.
        SwiftMessages.show(view: view)
        */
    }
